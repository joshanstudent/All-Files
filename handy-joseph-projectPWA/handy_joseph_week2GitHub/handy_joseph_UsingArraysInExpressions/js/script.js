//Using Arrays in Expressions 

//declare the variable
var sodaCups = [135, 35, 350];

//assigned the strings to find the sum total 
var total = sodaCups[0] + sodaCups[1] + sodaCups[2];
console.log(total);