//Declare the three variables
var firstNumber = 15;
var secondNumber = 10;
var answer;

//Set answer variables to the results of the calculation of the expression
answer = 15 / 10;
alert(answer);


//Demonstrate using expressions in places you will use numbers or variables
alert(15 / 10);


//Use the two expressions that was stored in the variables 
answer = firstNumber / secondNumber;

//Value will be displayed from the answer variable using the alert function
alert(answer);